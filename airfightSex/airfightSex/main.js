require('UIColor');
defineClass('ViewController', {
            
            initColor: function() {
            self.view().setBackgroundColor(UIColor.blueColor());
            
            }
            
            });